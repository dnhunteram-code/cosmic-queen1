export default {

    // Heart
heartScale: 0.55,
    // Outline
outlineOffset: 0.45,    
    auraOffset: 0.32,
    auraRandom: 0.12,

    // Depth
    heartDepth: 0.8,
    outlineDepth: 1.0,
    auraDepth: 1.6,

    // Sizes
    pointSize: 0.055,
    outlineSize: 0.075,
    auraSize: 0.04,

    // Animation
    pulseSpeed: 2.4,
    pulseStrength: 0.025,

    positionY: 26

};